/**
 * IoC Management Repositories
 */

const iocRepository = require('./IoCRepository');
const sourceRepository = require('./SourceRepository');

module.exports = {
  iocRepository,
  sourceRepository
};
